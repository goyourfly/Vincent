<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.goyourfly.vincent.app">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.RecyclerViewSampleActivity" />
        <activity android:name=".activity.EndlessRecyclerViewSampleActivity" />
        <activity android:name=".activity.StaggeredRecyclerViewSampleActivity" />
        <activity android:name=".activity.GallerySampleActivity" />
        <activity android:name=".activity.SingleImageSampleActivity" />
        <activity android:name=".activity.GifSampleActivity" />
        <activity
            android:name=".activity.MainActivity"
            android:theme="@style/AppTheme.DarkActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>